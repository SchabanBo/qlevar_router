import 'package:flutter/material.dart';

class MobileSettingsView extends StatelessWidget {
  const MobileSettingsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Icon(Icons.settings),
    );
  }
}
